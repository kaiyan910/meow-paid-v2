# Task: Add react-i18next to UI text

## Changes

- Installed `i18next` and `react-i18next`
- Created `src/i18n/index.ts` — i18n initialisation (useInitReactI18next, lng: "en")
- Created `src/i18n/locales/en.json` — English translation strings for app and login namespaces
- Updated `src/main.tsx` — import `@/i18n` before app render
- Updated `src/App.tsx` — replaced hardcoded text with `t("app.title")` and `t("app.footer")`
- Updated `src/components/login-form.tsx` — replaced all hardcoded text with `t()` calls

## Result

Build passes, Biome lint clean.
