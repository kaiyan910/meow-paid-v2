#Test 1: Login Form

## Requirement
- use `vitetest` and `React Testing Library` to test the login form `login-form.tsx`
- test the `email` validation success and fail cases
- test the `password` validation success and fail cases
- test the login form submission success and fail cases 